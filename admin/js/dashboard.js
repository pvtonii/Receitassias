/* ============================================
   APP ADMIN - SETOR: Dashboard
   Edite SO este arquivo para mexer nesta tela.
============================================ */
const Dashboard = {
  async render(container) {
    container.innerHTML = "<h2 style=\"padding:20px\">Dashboard — em construcao</h2>";
  }
};

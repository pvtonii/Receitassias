/* ============================================
   APP CLIENTE - SETOR: Pedido
   Edite SO este arquivo para mexer nesta tela.
============================================ */
const Pedido = {
  async render(container) {
    container.innerHTML = "<h2 style=\"padding:20px\">Order — coming soon</h2>";
  }
};

/* ============================================
   APP CLIENTE - SETOR: Profile
   Edite SO este arquivo para mexer nesta tela.
============================================ */
const Profile = {
  async render(container) {
    container.innerHTML = "<h2 style=\"padding:20px\">Profile — em construcao</h2>";
  }
};

/* ============================================
   APP CLIENTE - SETOR: Menu
   Edite SO este arquivo para mexer nesta tela.
============================================ */
const Menu = {
  async render(container) {
    container.innerHTML = "<h2 style=\"padding:20px\">Menu — em construcao</h2>";
  }
};
